package home

import (
	"go-starter/internal/view/shared"
	"go-starter/internal/view"
)

templ Show(title string) {
	@shared.Base(title, true) {
		<p class="text-2xl">{ view.GetSessionAttributes(ctx).Email }</p>
	}
}
